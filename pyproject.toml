# Generated by Dependency and Version Management Advisor
# Author: Marc Rivero López
# License: GNU General Public License v3 (GPLv3)
# Date: 2025-10-29

[build-system]
requires = ["hatchling>=1.28.0"]
build-backend = "hatchling.build"

[project]
name = "surinort-ast"
version = "1.1.0"
description = "Production-grade Abstract Syntax Tree parser for IDS rules (Suricata/Snort) with comprehensive validation and serialization support"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "Marc Rivero López", email = "mriverolopez@gmail.com" }
]
maintainers = [
    { name = "Marc Rivero López", email = "mriverolopez@gmail.com" }
]
keywords = [
    "ids",
    "ips",
    "parser",
    "ast",
    "suricata",
    "snort",
    "security",
    "cybersecurity",
    "network-security",
    "intrusion-detection",
    "rule-parser",
    "signature-analysis",
    "threat-detection",
    "pcap",
    "network-monitoring",
    "malware-analysis"
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: OS Independent",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Compilers",
    "Topic :: Software Development :: Interpreters",
    "Topic :: System :: Networking :: Monitoring",
    "Typing :: Typed",
    "Natural Language :: English",
]

# Core production dependencies
dependencies = [
    "lark>=1.3.1",           # Modern parser with Earley/LALR support
    "pydantic>=2.12.5",      # Data validation with type hints (includes JSON serialization)
    "typer>=0.21.0",         # Modern CLI framework with type hints
    "jsonschema>=4.25.1",    # JSON Schema validation
]

[project.optional-dependencies]
# Optional serialization formats
serialization = [
    "msgpack>=1.1.2",        # Binary serialization (faster than JSON)
    "protobuf>=6.33.2",      # Protocol Buffers support
]

# Enhanced CLI output with rich formatting
cli-enhanced = [
    "rich>=14.2.0",          # Rich terminal output, colors, tables
]

# Security analysis tools
security = [
    "bandit>=1.9.2",         # Security vulnerability scanner
    "safety>=3.7.0",         # Dependency security checker
]

# Advanced analysis features
analysis = [
    "networkx>=3.6.1",       # Graph analysis for rule relationships
]

# Additional format support
formats = [
    "pyyaml>=6.0.3",         # YAML serialization support
    "python-magic>=0.4.27",  # File type detection
]

# Performance benchmarking (optional)
benchmark = [
    "pytest-benchmark>=5.2.3",  # Performance benchmarking for tests
]

# Development dependencies
dev = [
    # Testing
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "hypothesis>=6.148.8",   # Property-based testing

    # Linting and formatting (ruff replaces: flake8, black, isort, pyupgrade)
    "ruff>=0.14.10",

    # Type checking
    "mypy>=1.19.1",

    # Build and publishing
    "build>=1.3.0",
    "twine>=6.2.0",

    # Pre-commit hooks
    "pre-commit>=4.5.1",

    # Documentation
    "mkdocs-material>=9.7.1",
]

# All optional dependencies combined
all = [
    "surinort-ast[serialization,cli-enhanced,security,analysis,formats,benchmark,dev]",
]

[project.urls]
Homepage = "https://github.com/seifreed/surinort-ast"
Documentation = "https://seifreed.github.io/surinort-ast"
Repository = "https://github.com/seifreed/surinort-ast"
Issues = "https://github.com/seifreed/surinort-ast/issues"
Changelog = "https://github.com/seifreed/surinort-ast/blob/main/CHANGELOG.md"
Source = "https://github.com/seifreed/surinort-ast"
"Bug Tracker" = "https://github.com/seifreed/surinort-ast/issues"
"Release Notes" = "https://github.com/seifreed/surinort-ast/releases"
Discussions = "https://github.com/seifreed/surinort-ast/discussions"

[project.scripts]
surinort = "surinort_ast.cli:app"

# Plugin entry points for extensibility
# Third-party plugins can register themselves here
[project.entry-points."surinort_ast.plugins"]
# Example entry points (commented out - enable when plugins are ready):
# yaml_serializer = "surinort_yaml:YAMLSerializerPlugin"
# toml_serializer = "surinort_toml:TOMLSerializerPlugin"
# security_auditor = "surinort_security:SecurityAuditorPlugin"

# Ruff configuration (replaces flake8, black, isort)
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]
exclude = [
    "benchmarks",
    "examples",
    "check_circular_deps.py",
    "src/surinort_ast/serialization/protobuf/src",  # Duplicate directory (should be removed)
]

[tool.ruff.lint]
# Enable common linting rules
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "N",      # pep8-naming
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "SIM",    # flake8-simplify
    "RET",    # flake8-return
    "ARG",    # flake8-unused-arguments
    "PTH",    # flake8-use-pathlib
    "PL",     # pylint
    "RUF",    # Ruff-specific rules
]
ignore = [
    "E501",   # Line too long (handled by formatter)
    "PLR0913", # Too many arguments
    "PLR2004", # Magic value used in comparison
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG", "PLR2004", "N802", "PLC0415", "PTH123", "PLW2901"]  # Allow test-specific patterns

# Singledispatch pattern requires unused arguments for dispatch registration
"src/surinort_ast/printer/text_printer.py" = [
    "ARG001",  # Unused function argument (singledispatch pattern requirement)
    "PLR0911", # Too many returns (inherent to printer dispatch logic)
]
"src/surinort_ast/analysis/conflicts_outline.py" = [
    "N802",    # Visitor methods match AST node names
    "ARG001",  # Unused function arguments (visitor pattern requirement)
    "ARG002",  # Unused method arguments (visitor pattern requirement)
]
"src/surinort_ast/parsing/lark_parser.py" = [
    "ARG001",  # Unused arguments (Lark transformer interface compliance)
    "ARG002",  # Unused arguments (Lark transformer interface compliance)
]
"src/surinort_ast/parsing/mixins/content_transformer.py" = [
    "ARG002",  # Unused arguments (transformer mixin interface compliance)
]
"src/surinort_ast/parsing/mixins/options/generic_mixin.py" = [
    "ARG002",  # Unused arguments (mixin interface compliance)
]
"src/surinort_ast/query/parser.py" = [
    "ARG002",  # Unused arguments (parser interface compliance)
]
"src/surinort_ast/query/selectors.py" = [
    "ARG002",  # Unused arguments (selector interface compliance)
    "PLR0911", # Too many returns (inherent to selector matching logic)
    "PLR0912", # Too many branches (inherent to selector matching logic)
]
"src/surinort_ast/query/executor.py" = [
    "ARG001",  # Unused arguments (executor interface compliance)
    "ARG002",  # Unused arguments (executor interface compliance)
    "PLR0911", # Too many returns (inherent to query execution)
    "PLR0912", # Too many branches (inherent to query execution)
]
"src/surinort_ast/serialization/protobuf/serializer.py" = [
    "ARG001",  # Unused arguments (serializer interface compliance)
    "PLR0911", # Too many returns (inherent to serialization logic)
]
"src/surinort_ast/streaming/processor.py" = [
    "ARG002",  # Unused arguments (processor interface compliance)
]

# Import outside top-level to avoid circular dependencies (by design)
"src/surinort_ast/streaming/parser.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
    "PLR0912",  # Too many branches (inherent to streaming parser logic)
    "PLR0915",  # Too many statements (inherent to streaming parser logic)
]
"src/surinort_ast/plugins/loader.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/plugins/registry.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/analysis/*.py" = [
    "PLC0415",  # Lazy imports to avoid circular dependencies
]
"src/surinort_ast/query/*.py" = [
    "PLC0415",  # Lazy imports for optional dependencies and circular imports
]
"src/surinort_ast/builder/option_builders.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/parsing/factory.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/parsing/transformer.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/parsing/mixins/options/metadata_mixin.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/cli/shared.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
]
"src/surinort_ast/cli/commands/*.py" = [
    "PLC0415",  # Import inside function (avoids circular imports and optional deps)
]

# Complex logic inherent to parsing/validation/CLI
"src/surinort_ast/api/parsing.py" = [
    "PLC0415",  # Import inside function (avoids circular imports)
    "PLR0912",  # Too many branches (inherent to API validation)
]
"src/surinort_ast/builder/rule_builder.py" = [
    "PLR0911",  # Too many returns (inherent to builder pattern)
]
"src/surinort_ast/core/nodes.py" = [
    "UP007",    # Runtime type aliases need Union syntax for Pydantic
]
"src/surinort_ast/cli/main.py" = [
    "PLR0912",  # Too many branches (CLI command dispatch)
    "PLR0915",  # Too many statements (CLI setup)
    "PLC0415",  # Import inside function (optional dependencies)
    "ARG001",   # Unused arguments (CLI framework callbacks)
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# MyPy configuration
[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_any_generics = true
disallow_subclassing_any = true
disallow_untyped_calls = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

# Pydantic plugin for mypy
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

# Protobuf generated code lacks type annotations (unfixable without regeneration)
[[tool.mypy.overrides]]
module = "surinort_ast.serialization.protobuf.ast_pb2"
disable_error_code = ["no-untyped-call", "attr-defined", "no-any-return", "unused-ignore"]

# Duplicate protobuf generated file (should be cleaned up)
[[tool.mypy.overrides]]
module = "surinort_ast.serialization.protobuf.src.surinort_ast.serialization.protobuf.ast_pb2"
disable_error_code = ["no-untyped-call", "attr-defined", "no-any-return", "unused-ignore"]

[[tool.mypy.overrides]]
module = "surinort_ast.serialization.protobuf.serializer"
disable_error_code = ["attr-defined", "no-any-return", "unused-ignore"]

# Streaming parser: union-attr errors are false positives with discriminated unions
[[tool.mypy.overrides]]
module = "surinort_ast.streaming.parser"
disable_error_code = ["union-attr", "arg-type"]

[[tool.mypy.overrides]]
module = "surinort_ast.streaming.processor"
disable_error_code = ["union-attr", "arg-type", "unused-ignore"]

# Parsing module: untyped decorators from Lark library (unfixable without library changes)
[[tool.mypy.overrides]]
module = "surinort_ast.parsing.parser"
disable_error_code = ["name-defined", "unused-ignore"]

# Query system: union-attr false positives with selector type narrowing
[[tool.mypy.overrides]]
module = "surinort_ast.query.executor"
disable_error_code = ["union-attr"]

# Plugin system: complex type union compatibility
[[tool.mypy.overrides]]
module = "surinort_ast.plugins.loader"
disable_error_code = ["return-value"]

# Writers: generator type compatibility
[[tool.mypy.overrides]]
module = "surinort_ast.streaming.writers"
disable_error_code = ["misc"]

# Analysis strategies: union compatibility with base Option type
[[tool.mypy.overrides]]
module = "surinort_ast.analysis.strategies"
disable_error_code = ["arg-type"]

# Builder: union type handling for address expressions
[[tool.mypy.overrides]]
module = "surinort_ast.builder.rule_builder"
disable_error_code = ["assignment", "operator", "attr-defined"]

# CLI shared: untyped callback decorators
[[tool.mypy.overrides]]
module = "surinort_ast.cli.shared"
disable_error_code = ["no-untyped-def"]

# Lark decorator files: untyped decorators from Lark library (@v_args, etc.)
# These cannot be fixed without library type stubs, so misc errors are suppressed
[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.port_transformer"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.header_transformer"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.content_transformer"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.address_transformer"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.options.metadata_mixin"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.mixins.options.buffer_mixin"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.parsing.transformer"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.query.parser"
disable_error_code = ["misc"]

[[tool.mypy.overrides]]
module = "surinort_ast.cli.main"
disable_error_code = ["misc"]

# API parsing: return type compatibility
[[tool.mypy.overrides]]
module = "surinort_ast.api.parsing"
disable_error_code = ["no-any-return"]

# CLI plugins command: plugin type assignments
[[tool.mypy.overrides]]
module = "surinort_ast.cli.commands.plugins"
disable_error_code = ["assignment", "unused-ignore", "import-untyped", "name-defined"]

# Plugin interface: missing library stubs for internal module
[[tool.mypy.overrides]]
module = "surinort_ast.plugins.interface"
disable_error_code = ["import-untyped"]

# Pytest configuration
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
pythonpath = ["src"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--showlocals",
    "--cov=surinort_ast",
    "--cov-report=term-missing",
    "--cov-report=html",
    "--cov-report=xml",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
    "golden: marks tests that use real-world rule files (35k+ rules)",
]

# Coverage configuration
[tool.coverage.run]
branch = true
source = ["surinort_ast"]
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__pycache__/*",
]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

# Hatchling configuration
[tool.hatch.build.targets.wheel]
packages = ["src/surinort_ast"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/docs",
    "/README.md",
    "/LICENSE",
    "/CHANGELOG.md",
    "/pyproject.toml",
    "/requirements.txt",
    "/requirements-dev.txt",
]
exclude = [
    "/.git",
    "/.github",
    "/.vscode",
    "/.idea",
    "/dist",
    "/build",
    "/__pycache__",
    "/*.egg-info",
    "/.pytest_cache",
    "/.mypy_cache",
    "/.ruff_cache",
    "/htmlcov",
    "/.coverage",
    "/venv",
    "/env",
]

# Bandit security configuration
[tool.bandit]
targets = ["src"]
exclude_dirs = ["tests", "venv", ".venv", "env", "benchmarks", "examples"]
# B101: assert_used - Allow asserts in non-production code paths
skips = ["B101"]
