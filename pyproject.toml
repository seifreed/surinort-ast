# Generated by Dependency and Version Management Advisor
# Author: Marc Rivero López
# License: GNU General Public License v3 (GPLv3)
# Date: 2025-10-29

[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "surisnort-ast"
version = "0.1.0"
description = "Production-grade Abstract Syntax Tree parser for IDS rules (Suricata/Snort) with comprehensive validation and serialization support"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "GPL-3.0-or-later" }
authors = [
    { name = "Marc Rivero López", email = "mriverolopez@gmail.com" }
]
maintainers = [
    { name = "Marc Rivero López", email = "mriverolopez@gmail.com" }
]
keywords = [
    "ids",
    "ips",
    "parser",
    "ast",
    "suricata",
    "snort",
    "security",
    "cybersecurity",
    "network-security",
    "intrusion-detection",
    "rule-parser",
    "signature-analysis",
    "threat-detection",
    "pcap",
    "network-monitoring",
    "malware-analysis"
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
    "Operating System :: OS Independent",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Security",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Compilers",
    "Topic :: Software Development :: Interpreters",
    "Topic :: System :: Networking :: Monitoring",
    "Typing :: Typed",
    "Natural Language :: English",
]

# Core production dependencies
dependencies = [
    "lark>=1.3.1",           # Modern parser with Earley/LALR support
    "pydantic>=2.12.3",      # Data validation with type hints (includes JSON serialization)
    "typer>=0.20.0",         # Modern CLI framework with type hints
    "jsonschema>=4.25.1",    # JSON Schema validation
]

[project.optional-dependencies]
# Optional serialization formats
serialization = [
    "msgpack>=1.1.2",        # Binary serialization (faster than JSON)
    "protobuf>=6.33.0",      # Protocol Buffers support
]

# Development dependencies
dev = [
    # Testing
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "hypothesis>=6.142.4",   # Property-based testing

    # Linting and formatting (ruff replaces: flake8, black, isort, pyupgrade)
    "ruff>=0.14.2",

    # Type checking
    "mypy>=1.18.2",

    # Build and publishing
    "build>=1.3.0",
    "twine>=6.2.0",

    # Pre-commit hooks
    "pre-commit>=4.3.0",

    # Documentation
    "mkdocs-material>=9.6.22",
]

# All optional dependencies combined
all = [
    "surisnort-ast[serialization,dev]",
]

[project.urls]
Homepage = "https://github.com/seifreed/surinort-ast"
Documentation = "https://seifreed.github.io/surinort-ast"
Repository = "https://github.com/seifreed/surinort-ast"
Issues = "https://github.com/seifreed/surinort-ast/issues"
Changelog = "https://github.com/seifreed/surinort-ast/blob/main/CHANGELOG.md"
Source = "https://github.com/seifreed/surinort-ast"
"Bug Tracker" = "https://github.com/seifreed/surinort-ast/issues"
"Release Notes" = "https://github.com/seifreed/surinort-ast/releases"
Discussions = "https://github.com/seifreed/surinort-ast/discussions"

[project.scripts]
surinort = "surinort_ast.cli:app"

# Ruff configuration (replaces flake8, black, isort)
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
# Enable common linting rules
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "N",      # pep8-naming
    "UP",     # pyupgrade
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "SIM",    # flake8-simplify
    "RET",    # flake8-return
    "ARG",    # flake8-unused-arguments
    "PTH",    # flake8-use-pathlib
    "PL",     # pylint
    "RUF",    # Ruff-specific rules
]
ignore = [
    "E501",   # Line too long (handled by formatter)
    "PLR0913", # Too many arguments
    "PLR2004", # Magic value used in comparison
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["ARG", "PLR2004"]  # Allow unused args and magic values in tests

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

# MyPy configuration
[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_any_generics = true
disallow_subclassing_any = true
disallow_untyped_calls = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true

# Pydantic plugin for mypy
plugins = ["pydantic.mypy"]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

# Pytest configuration
[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
pythonpath = ["src"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--showlocals",
    "--cov=surinort_ast",
    "--cov-report=term-missing",
    "--cov-report=html",
    "--cov-report=xml",
]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
    "integration: marks tests as integration tests",
]

# Coverage configuration
[tool.coverage.run]
branch = true
source = ["surinort_ast"]
omit = [
    "*/tests/*",
    "*/test_*.py",
    "*/__pycache__/*",
]

[tool.coverage.report]
precision = 2
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "@abstractmethod",
]

# Hatchling configuration
[tool.hatch.build.targets.wheel]
packages = ["src/surinort_ast"]

[tool.hatch.build.targets.sdist]
include = [
    "/src",
    "/tests",
    "/docs",
    "/README.md",
    "/LICENSE",
    "/CHANGELOG.md",
    "/pyproject.toml",
    "/requirements.txt",
    "/requirements-dev.txt",
]
exclude = [
    "/.git",
    "/.github",
    "/.vscode",
    "/.idea",
    "/dist",
    "/build",
    "/__pycache__",
    "/*.egg-info",
    "/.pytest_cache",
    "/.mypy_cache",
    "/.ruff_cache",
    "/htmlcov",
    "/.coverage",
    "/venv",
    "/env",
]

# Bandit security configuration
[tool.bandit]
targets = ["src"]
exclude_dirs = ["tests", "venv", ".venv", "env"]
skips = []
# B101: assert_used - Allow asserts in non-production code paths
# B601: paramiko_calls - Not applicable
tests = ["B201", "B301", "B302", "B303", "B304", "B305", "B306", "B307", "B308", "B309", "B310", "B311", "B312", "B313", "B314", "B315", "B316", "B317", "B318", "B319", "B320", "B321", "B322", "B323", "B324", "B325", "B401", "B402", "B403", "B404", "B405", "B406", "B407", "B408", "B409", "B410", "B411", "B412", "B413", "B501", "B502", "B503", "B504", "B505", "B506", "B507", "B508", "B509"]
